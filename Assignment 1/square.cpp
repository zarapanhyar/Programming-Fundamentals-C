// Online C compiler to run C program online
#include <stdio.h>

int main() {
    
    int number, square;
    printf("Enter a Number: ");
    scanf("%d",&number);
    square = number*number;
    printf("\n The square of %d is %d", number, square);

    return 0;
}